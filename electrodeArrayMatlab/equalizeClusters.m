clear
clc
matrix = [...
    376, 378, 397; ...
    427, 388, 397; ...
    418, 425, 394];
ave = sum(sum(matrix))/9;
List = reshape(matrix,[1,9]);
[~,idx] = max(List);
maxR = mod(idx-1,3)+1;
maxC = ceil(idx/3);
idx = [...
    mod(maxR-1-1,3)+1, maxC;...
    mod(maxR-1+1,3)+1, maxC;...
    maxR, mod(maxC-1-1,3)+1;...
    maxR, mod(maxC-1+1,3)+1];
matrix(idx)